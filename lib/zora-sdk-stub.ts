// Lightweight stub for '@zoralabs/coins-sdk' to avoid build-time issues.
// We only use it (optionally) to set an API key, so a no-op implementation is sufficient.

export function setApiKey(_apiKey: string): void {
  // Intentionally empty – this is just to satisfy imports in server code.
}

// If the real SDK were used elsewhere, you could add more stubbed exports here as needed.













